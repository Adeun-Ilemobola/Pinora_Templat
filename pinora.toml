schema_version = 1

[project]
name = "Pinora_Template"
version = "0.5.0-alpha"

[paths]
firmware = "Firmware_Templates"
ui = "UI"
config = ".espConfig"

# [device]
# board = "esp32s3"
# port = "auto"
# baud_rate = 115200
