export struct ServoState {
    id: string,
    has-angle: bool,
    angle: int,
    has-min-pivot: bool,
    min-pivot: int,
    has-max-pivot: bool,
    max-pivot: int,
    has-offset: bool,
    offset: int,
}
